# MicX v1.3 开发执行计划

**版本**: v1.3.0
**基于 PRD**: `2026-04-17-micx-v1.3-prd.md`
**创建日期**: 2026-04-17
**状态**: 待启动

---

## 一、技术架构

### 1.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (Next.js)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │  Task UI     │  │ Knowledge UI  │  │  Input Box       │  │
│  │  /tasks      │  │ /knowledge    │  │  /schedule cmd   │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Gateway API (FastAPI)                   │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │  Task Router    │  │ Knowledge Router │                  │
│  │  POST/GET/PUT   │  │ CRUD + Search    │                  │
│  └─────────────────┘  └─────────────────┘                  │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │  Scheduler      │  │  Embedding Svc   │                  │
│  │  APScheduler     │  │  sentence-transformers │
│  └─────────────────┘  └─────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
┌─────────────────────────┐     ┌─────────────────────────────┐
│    SQLite               │     │    ChromaDB                  │
│    - scheduled_tasks    │     │    - Vectors                 │
│    - task_executions    │     │    - Collection per KB       │
│    - knowledge_bases    │     │                              │
│    - documents          │     │                              │
│    - document_chunks    │     │                              │
└─────────────────────────┘     └─────────────────────────────┘
```

### 1.2 数据模型

#### 1.2.1 定时任务相关

**scheduled_tasks 表**:
```sql
CREATE TABLE scheduled_tasks (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT,
    trigger_type TEXT NOT NULL,  -- 'cron', 'interval', 'one_time'
    trigger_config TEXT NOT NULL,  -- JSON: {"cron": "0 9 * * *", "timezone": "Asia/Shanghai"}
    prompt_template TEXT NOT NULL,
    model_name TEXT,
    skill_names TEXT,  -- JSON array
    notification_config TEXT,  -- JSON
    output_config TEXT,  -- JSON
    status TEXT DEFAULT 'active',  -- 'active', 'paused', 'completed'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**task_executions 表**:
```sql
CREATE TABLE task_executions (
    id TEXT PRIMARY KEY,
    task_id TEXT NOT NULL,
    thread_id TEXT,
    status TEXT NOT NULL,  -- 'pending', 'running', 'success', 'failed'
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    result_summary TEXT,
    error_message TEXT,
    token_used INTEGER DEFAULT 0,
    FOREIGN KEY (task_id) REFERENCES scheduled_tasks(id)
);
```

#### 1.2.2 RAG 知识库相关

**knowledge_bases 表**:
```sql
CREATE TABLE knowledge_bases (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT,
    embedding_model TEXT DEFAULT 'sentence-transformers/all-MiniLM-L6-v2',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

**documents 表**:
```sql
CREATE TABLE documents (
    id TEXT PRIMARY KEY,
    knowledge_base_id TEXT NOT NULL,
    original_name TEXT NOT NULL,
    file_type TEXT NOT NULL,
    file_size INTEGER,
    storage_path TEXT NOT NULL,
    status TEXT DEFAULT 'processing',  -- 'processing', 'ready', 'error'
    chunk_count INTEGER DEFAULT 0,
    token_count INTEGER DEFAULT 0,
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    processed_at TIMESTAMP,
    FOREIGN KEY (knowledge_base_id) REFERENCES knowledge_bases(id)
);
```

**document_chunks 表**:
```sql
CREATE TABLE document_chunks (
    id TEXT PRIMARY KEY,
    document_id TEXT NOT NULL,
    chunk_index INTEGER NOT NULL,
    content TEXT NOT NULL,
    token_count INTEGER NOT NULL,
    embedding_id TEXT NOT NULL,  -- ChromaDB vector ID
    metadata TEXT,  -- JSON: page number, position, etc.
    FOREIGN KEY (document_id) REFERENCES documents(id)
);
```

### 1.3 新增依赖

**Python 依赖** (`backend/pyproject.toml`):
```toml
[project.optional-dependencies]
rag = [
    "chromadb>=0.4.0",
    "sentence-transformers>=2.2.0",
    "pymupdf>=1.23.0",
    "python-docx>=1.0.0",
    "pandas>=2.0.0",
    "langchain>=0.1.0",
    "langchain-community>=0.0.0",
]
scheduler = [
    "apscheduler>=3.10.0",
]
```

---

## 二、任务分解

### Phase 1: 基础设施 (Day 1)

#### T1.1 数据库迁移
**文件**: `backend/app/gateway/database.py`
**任务**:
- [ ] 添加 scheduled_tasks 表
- [ ] 添加 task_executions 表
- [ ] 添加 knowledge_bases 表
- [ ] 添加 documents 表
- [ ] 添加 document_chunks 表
- [ ] 创建数据库迁移脚本

#### T1.2 新增 API Router
**文件**: `backend/app/gateway/routers/tasks.py`, `knowledge.py`
**任务**:
- [ ] 创建 TaskRouter
- [ ] 创建 KnowledgeRouter
- [ ] 注册到 main.py

#### T1.3 配置项
**文件**: `backend/packages/harness/deerflow/config/app_config.py`
**任务**:
- [ ] 添加 RAG 配置项（嵌入模型、向量化参数）
- [ ] 添加 Scheduler 配置项（是否启用、最大并发任务数）

---

### Phase 2: 定时任务系统 (Day 2-3)

#### T2.1 定时任务 CRUD
**文件**: `backend/app/gateway/routers/tasks.py`
**API**:
```python
@router.post("/tasks")
async def create_task(task: ScheduledTaskCreate) -> ScheduledTask

@router.get("/tasks")
async def list_tasks() -> list[ScheduledTask]

@router.get("/tasks/{task_id}")
async def get_task(task_id: str) -> ScheduledTask

@router.put("/tasks/{task_id}")
async def update_task(task_id: str, task: ScheduledTaskUpdate) -> ScheduledTask

@router.delete("/tasks/{task_id}")
async def delete_task(task_id: str) -> None
```

#### T2.2 任务执行核心
**文件**: `backend/app/gateway/services/scheduler_service.py`
**任务**:
- [ ] SchedulerService 类封装 APScheduler
- [ ] add_job(), remove_job(), pause_job(), resume_job()
- [ ] get_next_run_time()
- [ ] 任务到期触发逻辑

#### T2.3 任务执行器
**文件**: `backend/app/gateway/services/task_executor.py`
**任务**:
- [ ] 创建新 thread 用于任务执行
- [ ] 调用 LangGraph API
- [ ] 保存执行结果
- [ ] 发送通知

#### T2.4 手动触发 API
**文件**: `backend/app/gateway/routers/tasks.py`
**任务**:
- [ ] POST `/tasks/{id}/run` - 立即执行
- [ ] POST `/tasks/{id}/pause`
- [ ] POST `/tasks/{id}/resume`
- [ ] GET `/tasks/{id}/executions`

---

### Phase 3: 前端任务 UI (Day 4-5)

#### T3.1 任务列表页
**文件**: `frontend/src/app/workspace/tasks/page.tsx`
**任务**:
- [ ] 任务卡片组件
- [ ] 状态筛选（全部/活跃/暂停）
- [ ] 列表 API 集成

#### T3.2 创建/编辑任务页
**文件**: `frontend/src/app/workspace/tasks/new/page.tsx`, `[task_id]/page.tsx`
**任务**:
- [ ] 表单组件（名称、描述、触发器配置）
- [ ] Cron 表达式可视化编辑器
- [ ] Prompt 模板编辑器
- [ ] 技能多选
- [ ] 通知配置

#### T3.3 任务详情页
**文件**: `frontend/src/app/workspace/tasks/[task_id]/page.tsx`
**任务**:
- [ ] 任务信息展示
- [ ] 执行历史列表
- [ ] 手动操作按钮

#### T3.4 输入框集成
**文件**: `frontend/src/components/workspace/input-box.tsx`
**任务**:
- [ ] `/schedule` 指令检测
- [ ] 快捷创建任务入口

---

### Phase 4: RAG 核心 (Day 6-7)

#### T4.1 知识库 CRUD
**文件**: `backend/app/gateway/routers/knowledge.py`
**API**:
```python
@router.post("/knowledge")
async def create_knowledge_base(kb: KnowledgeBaseCreate) -> KnowledgeBase

@router.get("/knowledge")
async def list_knowledge_bases() -> list[KnowledgeBase]

@router.get("/knowledge/{kb_id}")
async def get_knowledge_base(kb_id: str) -> KnowledgeBase

@router.put("/knowledge/{kb_id}")
async def update_knowledge_base(kb_id: str, kb: KnowledgeBaseUpdate) -> KnowledgeBase

@router.delete("/knowledge/{kb_id}")
async def delete_knowledge_base(kb_id: str) -> None
```

#### T4.2 文档上传与处理
**文件**: `backend/app/gateway/services/document_processor.py`
**任务**:
- [ ] 文档上传 API（含文件存储）
- [ ] PDF 解析 (PyMuPDF)
- [ ] DOCX 解析 (python-docx)
- [ ] TXT/MD 解析（直接读取）
- [ ] CSV 解析 (pandas)
- [ ] 文本分块 (~500 tokens, 50 token overlap)
- [ ] 异步处理队列

#### T4.3 向量化与存储
**文件**: `backend/app/gateway/services/embedding_service.py`
**任务**:
- [ ] ChromaDB 初始化
- [ ] 嵌入模型加载 (sentence-transformers)
- [ ] 向量批量插入
- [ ] 删除时同步清理向量

#### T4.4 语义检索
**文件**: `backend/app/gateway/services/search_service.py`
**任务**:
- [ ] semantic_search() 函数
- [ ] top_k 检索
- [ ] 相似度阈值过滤
- [ ] 元信息返回

---

### Phase 5: 前端知识库 UI (Day 8-9)

#### T5.1 知识库首页
**文件**: `frontend/src/app/workspace/knowledge/page.tsx`
**任务**:
- [ ] 知识库列表
- [ ] 创建知识库入口
- [ ] 知识库卡片组件

#### T5.2 知识库详情页
**文件**: `frontend/src/app/workspace/knowledge/[kb_id]/page.tsx`
**任务**:
- [ ] 文档列表
- [ ] 上传入口
- [ ] 文档操作（删除、重新索引）

#### T5.3 文档上传组件
**文件**: `frontend/src/components/workspace/knowledge/document-upload.tsx`
**任务**:
- [ ] 文件选择
- [ ] 上传进度
- [ ] 处理状态展示

---

### Phase 6: Agent 集成 (Day 10)

#### T6.1 RAG 上下文注入
**文件**: `backend/packages/harness/deerflow/agents/lead_agent/prompt.py`
**任务**:
- [ ] 检测用户是否上传了文档
- [ ] 调用检索 API
- [ ] 构建 <context> 块
- [ ] 注入到 system prompt

#### T6.2 技能支持
**文件**: `backend/packages/harness/deerflow/knowledge/loader.py`
**任务**:
- [ ] KnowledgeLoader 类
- [ ] 支持在 skill 中加载用户知识库
- [ ] 自动 RAG 触发逻辑

---

### Phase 7: 集成测试 (Day 11-12)

#### T7.1 单元测试
**文件**: `backend/tests/test_scheduler_service.py`, `test_knowledge_service.py`
**任务**:
- [ ] SchedulerService 单元测试
- [ ] EmbeddingService 单元测试
- [ ] SearchService 单元测试

#### T7.2 集成测试
**任务**:
- [ ] 定时任务完整流程测试
- [ ] RAG 端到端测试（上传→检索→问答）
- [ ] API 接口测试

#### T7.3 前端测试
**任务**:
- [ ] 任务页面 E2E 测试
- [ ] 知识库页面 E2E 测试

---

### Phase 8: 上线准备 (Day 13-14)

#### T8.1 文档
**任务**:
- [ ] 用户文档（定时任务使用指南）
- [ ] 用户文档（RAG 知识库使用指南）
- [ ] API 文档更新

#### T8.2 配置
**任务**:
- [ ] docker-compose 配置更新
- [ ] 环境变量文档
- [ ] 默认配置优化

#### T8.3 部署验证
**任务**:
- [ ] Docker 镜像构建
- [ ] 本地 Docker Compose 启动验证
- [ ] 功能回归测试

---

## 三、详细任务清单

### 3.1 Backend 任务

| # | 任务 | 文件 | 估计工时 | 依赖 |
|---|------|------|---------|------|
| B1 | 数据库表创建 | `backend/app/gateway/database.py` | 2h | - |
| B2 | Task CRUD API | `backend/app/gateway/routers/tasks.py` | 4h | B1 |
| B3 | SchedulerService | `backend/app/gateway/services/scheduler_service.py` | 4h | B1 |
| B4 | TaskExecutor | `backend/app/gateway/services/task_executor.py` | 4h | B3 |
| B5 | 任务执行历史 API | `backend/app/gateway/routers/tasks.py` | 2h | B4 |
| B6 | Knowledge CRUD API | `backend/app/gateway/routers/knowledge.py` | 4h | B1 |
| B7 | DocumentProcessor | `backend/app/gateway/services/document_processor.py` | 6h | B1 |
| B8 | EmbeddingService | `backend/app/gateway/services/embedding_service.py` | 6h | B1 |
| B9 | SearchService | `backend/app/gateway/services/search_service.py` | 4h | B8 |
| B10 | LangGraph 集成 RAG | `backend/packages/harness/deerflow/agents/...` | 4h | B9 |
| B11 | 通知服务 | `backend/app/gateway/services/notification_service.py` | 2h | B4 |
| B12 | 配置项更新 | `backend/packages/harness/deerflow/config/` | 2h | - |

### 3.2 Frontend 任务

| # | 任务 | 文件 | 估计工时 | 依赖 |
|---|------|------|---------|------|
| F1 | 任务列表页 | `frontend/src/app/workspace/tasks/page.tsx` | 3h | B2 |
| F2 | 创建任务页 | `frontend/src/app/workspace/tasks/new/page.tsx` | 4h | B2 |
| F3 | 任务详情页 | `frontend/src/app/workspace/tasks/[task_id]/page.tsx` | 3h | F2 |
| F4 | 知识库首页 | `frontend/src/app/workspace/knowledge/page.tsx` | 3h | B6 |
| F5 | 知识库详情页 | `frontend/src/app/workspace/knowledge/[kb_id]/page.tsx` | 4h | B7 |
| F6 | 文档上传组件 | `frontend/src/components/...` | 3h | B7 |
| F7 | InputBox 集成 | `frontend/src/components/workspace/input-box.tsx` | 2h | F2 |
| F8 | API 类型定义 | `frontend/src/core/api/types.ts` | 2h | B2, B6 |
| F9 | E2E 测试 | `frontend/tests/` | 4h | F1-F7 |

### 3.3 测试任务

| # | 任务 | 文件 | 估计工时 | 依赖 |
|---|------|------|---------|------|
| T1 | 单元测试 (backend) | `backend/tests/test_*.py` | 8h | B1-B12 |
| T2 | API 测试 | `backend/tests/test_api/` | 4h | B1-B12 |
| T3 | E2E 测试 | `frontend/tests/` | 4h | F1-F9 |

---

## 四、日程安排

```
Day 1 (04/18): 基础设施
├── B1: 数据库表
├── B12: 配置项
└── T1.1: 数据库测试

Day 2 (04/19): 定时任务后端
├── B2: Task CRUD API
├── B3: SchedulerService
└── 开始 B4

Day 3 (04/20): 定时任务后端完成
├── B4: TaskExecutor (完成)
├── B5: 任务执行历史 API
├── B11: 通知服务
└── T1.2: Scheduler 测试

Day 4 (04/21): 前端任务 UI
├── F1: 任务列表页
├── F2: 创建任务页
└── F3: 任务详情页

Day 5 (04/22): 前端任务完善
├── F7: InputBox 集成
├── F8: API 类型定义
└── T2.1: Task API 测试

Day 6 (04/23): RAG 后端
├── B6: Knowledge CRUD API
├── B7: DocumentProcessor
└── 开始 B8

Day 7 (04/24): RAG 后端完成
├── B8: EmbeddingService (完成)
├── B9: SearchService (完成)
└── T1.3: RAG Service 测试

Day 8 (04/25): 前端知识库 UI
├── F4: 知识库首页
├── F5: 知识库详情页
└── F6: 文档上传组件

Day 9 (04/26): 前端完善
├── F9: E2E 测试骨架
├── T2.2: Knowledge API 测试
└── B10: LangGraph 集成开始

Day 10 (04/27): Agent 集成完成
├── B10: LangGraph 集成 RAG (完成)
└── T1.4: 集成测试开始

Day 11 (04/28): 测试
├── T1.4: 集成测试
├── T2: API 测试
└── Bug 修复

Day 12 (04/29): 测试完成
├── T3: E2E 测试
├── Bug 修复
└── 回归测试

Day 13 (04/30): 上线准备
├── 文档编写
├── Docker 配置验证
└── 部署测试

Day 14 (05/01): 发布
├── 功能验证
├── 发布准备
└── v1.3.0 标记
```

---

## 五、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| ChromaDB 向量检索性能 | 中 | 限制单知识库文档数，预加载嵌入模型 |
| APScheduler 多实例竞争 | 中 | 使用数据库锁，K8s 下用单副本 |
| 文档解析失败 | 中 | 提供错误详情，支持 txt/md 回退 |
| 定时任务积压 | 低 | 限制最大并发任务数 |
| 前端工期超期 | 中 | 优先完成核心功能，可选功能移到 v1.4 |

---

## 六、代码规范

### 6.1 Backend

- 遵循现有 `ruff.toml` 配置
- 新增 API 必须有 OpenAPI 文档注释
- 所有数据库操作使用 async
- 错误处理使用 try-except + 日志记录

### 6.2 Frontend

- 遵循现有 ESLint/Prettier 配置
- 组件放在 `components/workspace/` 对应子目录
- API 调用通过 `src/core/api/` 封装
- 国际化 key 放在 `src/core/i18n/` 对应文件

---

## 七、交付物清单

- [ ] `docs/plans/2026-04-17-micx-v1.3-prd.md` (已完成)
- [ ] `docs/plans/2026-04-17-micx-v1.3-execution.md` (本文件)
- [ ] 定时任务系统代码
- [ ] RAG 知识增强代码
- [ ] 前端 UI 代码
- [ ] 单元测试代码
- [ ] E2E 测试代码
- [ ] 用户文档
- [ ] API 文档更新

---

**文档版本**: 1.0
**最后更新**: 2026-04-17
**状态**: 待评审后启动
