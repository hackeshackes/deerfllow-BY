# MicX v1.4.2 执行计划 - 飞书全能力集成

**版本**: v1.4.2
**日期**: 2026-04-24
**技术方案**: lark-oapi SDK（B 方案）

---

## 一、任务拆分（13 个工具模块 + 1 个公共模块）

### 阶段一: 飞书云文档工具

**文件**: `backend/packages/harness/deerflow/community/feishu_doc_tools.py` (新建)

**工具**:
```python
@tool("feishu_doc_read")
def feishu_doc_read(document_id: str) -> str

@tool("feishu_doc_search")
def feishu_doc_search(query: str, count: int = 10) -> str

@tool("feishu_doc_meta")
def feishu_doc_meta(document_id: str) -> str
```

**API**: `client.docx.v1.document.get()`, `client.docx.v1.document.search()`

---

### 阶段二: 飞书多维表格工具

**文件**: `backend/packages/harness/deerflow/community/feishu_bitable_tools.py` (新建)

**工具**:
```python
@tool("feishu_bitable_read")
def feishu_bitable_read(bitable_id: str, table_id: str | None = None, record_count: int = 100) -> str

@tool("feishu_bitable_write")
def feishu_bitable_write(bitable_id: str, table_id: str, action: str, records: list[dict]) -> str
```

**API**: `client.bitable.v1.app.get()`, `client.bitable.v1.table.list()`, `client.bitable.v1.record.*`

---

### 阶段三: 飞书日历工具

**文件**: `backend/packages/harness/deerflow/community/feishu_calendar_tools.py` (新建)

**工具**:
```python
@tool("feishu_calendar_list")
def feishu_calendar_list(user_id: str | None = None) -> str

@tool("feishu_calendar_event_list")
def feishu_calendar_event_list(calendar_id: str, start_time: str, end_time: str) -> str

@tool("feishu_calendar_event_create")
def feishu_calendar_event_create(calendar_id: str, title: str, start_time: str, end_time: str, attendees: list[str] | None = None) -> str

@tool("feishu_calendar_freebusy")
def feishu_calendar_freebusy(user_ids: list[str], start_time: str, end_time: str) -> str
```

**API**: `client.calendar.v4.calendar.list()`, `client.calendar.v4.event.list/create()`, `client.calendar.v4.freebusy.query()`

---

### 阶段四: 飞书任务工具

**文件**: `backend/packages/harness/deerflow/community/feishu_task_tools.py` (新建)

**工具**:
```python
@tool("feishu_task_list")
def feishu_task_list(user_id: str | None = None, status: str | None = None) -> str

@tool("feishu_task_add")
def feishu_task_add(title: str, due: str | None = None, user_id: str | None = None) -> str

@tool("feishu_task_complete")
def feishu_task_complete(task_guid: str) -> str
```

**API**: `client.task.v1.tasks.list/create/patch()`

---

### 阶段五: 飞书通讯录工具

**文件**: `backend/packages/harness/deerflow/community/feishu_contact_tools.py` (新建)

**工具**:
```python
@tool("feishu_contact_user")
def feishu_contact_user(user_id: str | None = None, email: str | None = None) -> str

@tool("feishu_contact_dept")
def feishu_contact_dept(department_id: str) -> str
```

**API**: `client.contact.v3.user.get()`, `client.contact.v3.department.get()`

---

### 阶段六: 飞书消息增强工具

**文件**: `backend/packages/harness/deerflow/community/feishu_im_tools.py` (新建)

**工具**:
```python
@tool("feishu_send_message")
def feishu_send_message(chat_id: str, content: str, msg_type: str = "text") -> str

@tool("feishu_download_file")
def feishu_download_file(message_id: str, file_key: str, file_type: str) -> str
```

**API**: `client.im.v1.message.create()`, `client.im.v1.message_resource.get()`

---

### 阶段七: 飞书审批工具

**文件**: `backend/packages/harness/deerflow/community/feishu_approval_tools.py` (新建)

**工具**:
```python
@tool("feishu_approval_list")
def feishu_approval_list(user_id_type: str = "open_id") -> str

@tool("feishu_approval_get")
def feishu_approval_get(instance_id: str) -> str

@tool("feishu_approval_action")
def feishu_approval_action(instance_id: str, action: str, comment: str | None = None) -> str
# action: approve / reject / transfer
```

**API**: `client.approval.v4.instance.list/get()`, `client.approval.v4.instance.approve/reject/transfer()`

---

### 阶段八: 飞书邮件工具

**文件**: `backend/packages/harness/deerflow/community/feishu_mail_tools.py` (新建)

**工具**:
```python
@tool("feishu_mail_list")
def feishu_mail_list(mail_type: str = "inbox", page_size: int = 20) -> str

@tool("feishu_mail_get")
def feishu_mail_get(message_id: str) -> str

@tool("feishu_mail_send")
def feishu_mail_send(to: list[str], subject: str, content: str, cc: list[str] | None = None) -> str
```

**API**: `client.mail.v1.message.list/get/create()`

---

### 阶段九: 飞书电子表格工具

**文件**: `backend/packages/harness/deerflow/community/feishu_sheet_tools.py` (新建)

**工具**:
```python
@tool("feishu_sheet_read")
def feishu_sheet_read(spreadsheet_token: str, sheet_id: str) -> str

@tool("feishu_sheet_write")
def feishu_sheet_write(spreadsheet_token: str, sheet_id: str, range: str, values: list[list]) -> str

@tool("feishu_sheet_range")
def feishu_sheet_range(spreadsheet_token: str, sheet_id: str, range: str) -> str
```

**API**: `client.sheets.v2.spreadsheet.sheet.get()`, `client.sheets.v2.spreadsheet.values.put/get()`

---

### 阶段十: 飞书知识库工具

**文件**: `backend/packages/harness/deerflow/community/feishu_drive_tools.py` (新建)

**工具**:
```python
@tool("feishu_drive_file_list")
def feishu_drive_file_list(folder_token: str | None = None, page_size: int = 50) -> str

@tool("feishu_drive_file_meta")
def feishu_drive_file_meta(file_token: str) -> str

@tool("feishu_drive_file_download")
def feishu_drive_file_download(file_token: str) -> str
```

**API**: `client.drive.v1.files.list/get()`, `client.drive.v1.files.download()`

---

### 阶段十一: 飞书妙记工具

**文件**: `backend/packages/harness/deerflow/community/feishu_minutes_tools.py` (新建)

**工具**:
```python
@tool("feishu_minutes_get")
def feishu_minutes_get(minutes_id: str) -> str

@tool("feishu_minutes_content")
def feishu_minutes_content(minutes_id: str) -> str
```

**API**: `client.minutes.v1.minutes.get()`

---

### 阶段十二: 飞书 OKR 工具

**文件**: `backend/packages/harness/deerflow/community/feishu_okr_tools.py` (新建)

**工具**:
```python
@tool("feishu_okr_period_list")
def feishu_okr_period_list(user_id: str | None = None) -> str

@tool("feishu_okr_get")
def feishu_okr_get(user_id: str, period_id: str) -> str

@tool("feishu_okr_progress")
def feishu_okr_progress(user_id: str, period_ids: list[str]) -> str
```

**API**: `client.okr.v1.period.list()`, `client.okr.v1.okr.get()`, `client.okr.v1.progress.list()`

---

### 阶段十三: 飞书考勤工具

**文件**: `backend/packages/harness/deerflow/community/feishu_attendance_tools.py` (新建)

**工具**:
```python
@tool("feishu_attendance_record")
def feishu_attendance_record(user_id: str, start_date: str, end_date: str) -> str

@tool("feishu_attendance_group")
def feishu_attendance_group(group_id: str) -> str
```

**API**: `client.attendance.v1.user_stats.get()`, `client.attendance.v1.group.get()`

---

### 阶段十四: 公共工具模块

**文件**: `backend/packages/harness/deerflow/community/feishu_utils.py` (新建)

**功能**:
- `_get_feishu_client()` - 获取 lark-oapi client
- `_check_feishu_enabled()` - 检查飞书是否启用
- `_handle_response()` - 统一响应处理
- 错误格式化和日志记录

---

## 二、文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `feishu_doc_tools.py` | 新建 | 云文档工具 (3) |
| `feishu_bitable_tools.py` | 新建 | 多维表格工具 (2) |
| `feishu_calendar_tools.py` | 新建 | 日历工具 (4) |
| `feishu_task_tools.py` | 新建 | 任务工具 (3) |
| `feishu_contact_tools.py` | 新建 | 通讯录工具 (2) |
| `feishu_im_tools.py` | 新建 | 消息增强工具 (2) |
| `feishu_approval_tools.py` | 新建 | 审批工具 (3) |
| `feishu_mail_tools.py` | 新建 | 邮件工具 (3) |
| `feishu_sheet_tools.py` | 新建 | 电子表格工具 (3) |
| `feishu_drive_tools.py` | 新建 | 知识库工具 (3) |
| `feishu_minutes_tools.py` | 新建 | 妙记工具 (2) |
| `feishu_okr_tools.py` | 新建 | OKR 工具 (3) |
| `feishu_attendance_tools.py` | 新建 | 考勤工具 (2) |
| `feishu_utils.py` | 新建 | 公共方法 |
| `feishu_tools.py` | 已有 | v1.4.1 消息读取 |

**总计**: 13 个新建文件，36 个工具函数

---

## 三、时间估算

| 阶段 | 任务 | 工具数 | 预计时间 |
|------|------|--------|----------|
| 阶段一 | 飞书云文档 | 3 | 0.25 天 |
| 阶段二 | 飞书多维表格 | 2 | 0.25 天 |
| 阶段三 | 飞书日历 | 4 | 0.25 天 |
| 阶段四 | 飞书任务 | 3 | 0.25 天 |
| 阶段五 | 飞书通讯录 | 2 | 0.25 天 |
| 阶段六 | 飞书消息增强 | 2 | 0.25 天 |
| 阶段七 | 飞书审批 | 3 | 0.25 天 |
| 阶段八 | 飞书邮件 | 3 | 0.25 天 |
| 阶段九 | 飞书电子表格 | 3 | 0.25 天 |
| 阶段十 | 飞书知识库 | 3 | 0.25 天 |
| 阶段十一 | 飞书妙记 | 2 | 0.25 天 |
| 阶段十二 | 飞书 OKR | 3 | 0.25 天 |
| 阶段十三 | 飞书考勤 | 2 | 0.25 天 |
| 阶段十四 | 公共模块 + 整合 | - | 0.25 天 |
| 阶段十五 | 测试验证 | - | 0.5 天 |
| **总计** | | **36** | **3.75 天** |

---

## 四、依赖更新

无需新增依赖，lark-oapi 已在 v1.4.1 中引入。如需更新版本：

```bash
cd backend
uv add lark-oapi
```

---

## 五、测试验证

### 后端测试

```bash
cd backend
uv run ruff check app/ packages/ --no-fix
uv run pytest tests/ -x -q --tb=line
```

### 前端构建

```bash
cd frontend
pnpm typecheck
pnpm build
```

---

## 六、风险点

1. **飞书权限范围**：工具能访问的内容受飞书应用权限和用户权限双重限制
2. **API 限流**：大量调用需处理限流错误，添加重试逻辑
3. **字段类型复杂性**：多维表格、任务等字段类型多样，需要处理不同类型序列化
4. **token 管理**：确保 tenant_access_token 有效期内处理，SDK 内置 token 管理

---

## 七、后续版本预告

### v1.4.3
- 对话标签
- 对话搜索升级
