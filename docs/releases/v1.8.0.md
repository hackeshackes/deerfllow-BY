# v1.8.0 Release Notes

**Tag:** `v1.8.0` (HEAD `af6596cf`)
**Released:** 2026-09-18
**Type:** minor (model auto-discovery, IM channel config, policy publish/audit, GCS/Aliyun replication, test-in-zero)
**Builds on:** `v1.7.0` (commit `2b45f4ac`)

---

## What ships in this tag

Closing-stable delta from `v1.7.0` to `af6596cf`. Features the model-management
increase (auto-discovery + mainstream presets), per-channel config persistence +
Slack socket-mode toggle, policy publish/audit, multi-region replication
adapters (GCS / Aliyun), Canvas executor wiring, and a fully green backend suite.

| # | Scope | Highlights |
|---|---|---|
| 1 | Model auto-discovery (M2) | `POST /api/admin/models/inspect` (OpenAI-compat / Anthropic / Gemini, SSRF-guarded, capability inference) + in-dialog "获取模型列表" |
| 2 | Mainstream provider presets (M1) | 15 provider families incl. local Ollama/vLLM/LM Studio |
| 3 | IM channel config (M3) | `PUT /api/channels/{type}` persistence + Slack socket-mode toggle |
| 4 | Policy publish + audit (M4.3) | `POST /policies/publish` + `GET /policies/audit` + editor audit tab |
| 5 | Replication adapters (M4.2) | `gcs_replicator`, `aliyun_oss_replicator` implementing `SecretReplicator` |
| 6 | Canvas executor wiring | prompt/branch/loop/agent/tool nodes executable (execute 503 fixed); default model -> DeepSeek (multi-container reachable) |

## Live regression (runtime, owner, nginx `:2026`)

Read coverage — 30/30 OK (incl. expected 404 for optional compat routes):
`session/me, models, mcp/presets, skills, custom/mine, user/skills, knowledge,
memory, memory/status, channels/, tasks, users, workflows, connectors,
workspaces, spaces, admin/{models,config,policies,monitoring,quota,secrets,
audit,token-usage}, channels/config, skills/bindings(404), voice(404),
ppt(404), assistants(404), runs(404)`.

Write/execute round-trips — all GREEN:
- Knowledge base create -> get -> delete
- Canvas workflow create -> get -> execute -> versions -> rollback -> delete; execute with/without workspace_id
- Custom skill create (full frontmatter) -> get -> delete
- Cross-workspace publish: thread create -> publish target ws -> 
- Policy publish -> 400 on empty (validation); audit -> 200 (returns `admin_policies.published` event)
- Model inspect (owner, bad-key -> graceful degrade)

Runtime health: 4 containers up, `/health` 200, no Traceback in 30m. Backend suite
`2541 passed / 0 failed`; frontend vitest 177 + build green.

## Notes / follow-up
- DR drill (kill-primary -> secondary ≤60s) requires real object-store credentials.
- Canvas `tool` node executes sync tools; async (langchain) tools surface a clean
  `runtime`-context error unless given a sync-capable registry adapter.
- Knowledge search returns empty until an embedding model is configured.
- Voice / PPT / Assistants / Runs remain optional/unmounted.

See [CHANGELOG.md](../../CHANGELOG.md) for the full entry.